The 4 year old tutorial gives the following incantation:

    docker-compose -f mongo.yaml up

But I am instead using the following:

    docker compose -f mongo.yaml up --detach

Notice that as of January 2025 there's no need to separately install [docker-compose].


